# include <stdio.h>

int main() {
    int i = 5;
    printf("Value of i : %d and its address: %u", i, &i);
    
    int *ptr_i;
    ptr_i = &i;
    printf("\nValue of ptr_i : %u and value pointed by the pointer is : %d", ptr_i, *ptr_i); 

    return 0;
}
***output***
Value of i : 5 and its address: 2753608836
Value of ptr_i : 2753608836 and value pointed by the pointer is : 5

=== Code Execution Successful ===
